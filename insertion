#include <stdio.h>
 int insertion(int arr[], int n,int pos,int value){
     for( int i = n; i>pos ;i--){
        arr[i]= arr[i-1] ; 
     }
     arr[pos] = value;
     printf("after insertion\n"); 
     for(int i = 0; i <= n; i++){
    
        printf("%d ", arr[i]);
    }
     
      
 }
int main() {
  int arr[6] = { 1 , 2 , 4 , 5};  
    int n = 4;
    int pos = 2;
    int value = 3;
   insertion( arr, n, pos, value);
    return 0;
    
}
